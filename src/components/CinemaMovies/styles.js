import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  moviesTitle: {
    fontSize: 28,
    marginLeft: 10,
    paddingBottom: 10,
    paddingTop: 10,
    fontWeight: 'bold',
  }
})
